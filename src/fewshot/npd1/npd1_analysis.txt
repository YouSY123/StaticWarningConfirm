First we inspect the function:
int foo(int a, int b) {
    Node *p = maybe_get(a);
    if (b < 0) {
        return -1;
    }
    else{
        return p->value + b;
    }
}

Dereference: return p->value + b;
Alias of "p": []
Last assignment to "p": Node *p = maybe_get(a);

```json
{
  "Files": [
    "code.c"
  ],
  "Warning information":
  {
    "File name": ["code.c"],
    "Type": "null pointer dereference",
    "Variable name": "p",
    "Line number": [25],
    "Confirmation conditions": {
      "1": {
        "target": "return value of "maybe_get(a)" and the else-branch is entered",
        "description": "There exists one call of function "foo" defined at code.c:19, maybe_get(a) at code.cpp:20 return a null pointer and the else-branch at code.c:24 is entered."
      }
    }
  }
}
```