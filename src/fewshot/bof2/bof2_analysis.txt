We inspect the function:
    char* write_buffer(int len, const char* dst){

        if(len > BUFFER_SIZE || len < 0){
            return nullptr;
        }

        int idx;
        for(idx = 0; idx < len; ++idx){
            buff[idx] = dst[idx];
        }
        buff[idx] = '\0';
    }

we can easily conclude that idx=len when reaching code.cpp:22:"buff[idx] = '\0';".
The array "buff" in class "Buffer" is declared by "char buff[BUFFER_SIZE];", so that if idx=BUFFER_SIZE, buffer overflow happens.

```json
{
  "Files": [
    "code.cpp"
  ],
  "Warning information":
  {
    "File name": ["code.cpp"],
    "Type": "buffer overflow",
    "Variable name": "buff",
    "Line number": [22],
    "Confirmation conditions": {
      "1": {
        "target": "The value of "len"",
        "description": "There exists some call of write_buffer defined at code.cpp:12 where "len" equals "BUFFER_SIZE"."
      }
    }
  }
}